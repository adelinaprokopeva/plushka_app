# Основные библиотеки
PyQt6==6.6.0             # Фреймворк для GUI
PyQt6-Qt6==6.6.0         # Qt6 биндинги для PyQt6
PyQt6-sip==13.6.0        # Биндинг для взаимодействия Python и Qt
python-dateutil==2.8.2   # Работа с датами (для модуля мероприятий)

# Дополнительные инструменты (опционально)
pyinstaller==6.0.0       # Для сборки в exe (если нужно)
pytest==7.4.0            # Тестирование (если будет использоваться)
# Для ORM (если замените прямой SQL на SQLAlchemy)
SQLAlchemy==2.0.25

# Для API-интеграций (например, с Google Calendar)
requests==2.31.0
PyQt6==6.6.0
PyQt6-Qt6==6.6.0
PyQt6-sip==13.6.0
Pillow==10.1.0  # Для работы с изображениями